import swaggerJSDoc from "swagger-jsdoc";

const options: swaggerJSDoc.Options = {
  definition: {
    openapi: "3.0.0",
    info: {
      title: "Libertant API",
      version: "1.0.0",
    },
        components: {
      securitySchemes: {
        bearerAuth: {
          type: "http",
          scheme: "bearer",
          bearerFormat: "JWT",
        },
      },
    },
    security: [
      {
        bearerAuth: [],
      },
    ],
  },
  apis: ["./src/**/*.routes.ts","./src/shared/*components.schema.ts"], // files containing swagger comments
};

export const swaggerSpec = swaggerJSDoc(options);